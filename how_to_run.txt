This script uses pipenv make sure its installed:
$ pip3 install pipenv
To activate this project's virtualenv, run 
$ pipenv shell
Alternatively, run a command inside the virtualenv with 
$ pipenv run
